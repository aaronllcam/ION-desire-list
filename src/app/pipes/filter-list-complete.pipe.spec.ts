import { FilterListCompletePipe } from './filter-list-complete.pipe';

describe('FilterListCompletePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterListCompletePipe();
    expect(pipe).toBeTruthy();
  });
});
